# -----------------------------
# SozoGraph v1 configuration
# -----------------------------

# Gemini API key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: default Gemini model for extraction
# Recommended: fast + reasoning-capable
SOZOGRAPH_EXTRACTOR_MODEL=gemini-3-flash

# Optional: enable Gemini fallback summarization for messy objects
# true | false
SOZOGRAPH_ENABLE_FALLBACK_SUMMARIZER=true

# Optional: maximum characters allowed for a single Interaction.text
SOZOGRAPH_MAX_INTERACTION_CHARS=10000

# Optional: maximum characters for exported context
SOZOGRAPH_DEFAULT_CONTEXT_BUDGET=8000
